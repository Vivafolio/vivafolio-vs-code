name: crystal-basic
version: 0.1.0
description: Basic Crystal project for LSP connectivity

targets:
  crystal-basic:
    main: src/main.cr

dependencies:
  # No dependencies needed for basic connectivity test
