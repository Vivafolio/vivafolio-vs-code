# mySolidBlock

A SolidJS-based Block Protocol block (no React). Uses Vite + vite-plugin-solid.

- Dev: vite dev server
- Build: static assets in `dist/`

## Usage

- npm install
- npm run dev
- npm run build

The block renders a simple greeting and simulates an entity update.